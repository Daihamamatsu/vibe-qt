[pytest]
; Django 設定モジュール（pytest-django が使用）
DJANGO_SETTINGS_MODULE = stockapp.settings
; バックエンド（Django プロジェクト）を import 可能にする
; （pytest-django は conftest より前に設定を読み込むため、ini 側で明示）
pythonpath = stock-app/backend
; テストの探索先
testpaths = tests
python_files = test_*.py
